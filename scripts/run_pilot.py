"""CLI script to run a small pilot experiment.

Will run the experiment pipeline over a small sample of MedQA questions and
a subset of conditions, for quick iteration before a full experiment run.

TODO: parse CLI args (sample size, conditions to run, output dir).
TODO: main() -> loads config, builds ExperimentRunner, runs pilot, prints summary.
"""
