"""Prompt templates for answer generation.

Will define the prompt templates that assemble a question and retrieved
context into a final prompt for the remote LLM.

TODO: build_answer_prompt(question, context_chunks) -> str prompt.
TODO: build_reformulation_prompt(question) -> str prompt.
TODO: build_verification_prompt(question, answer, context_chunks) -> str prompt.
"""
