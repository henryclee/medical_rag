"""Experiment condition definitions.

Will define the set of pipeline configurations (e.g. baseline, +reformulation,
+verification, combinations) evaluated by the experiment runner, loaded from
config/conditions.yaml.

TODO: class Condition: name + which modules/pipeline steps are enabled.
TODO: load_conditions(config) -> list[Condition].
"""
