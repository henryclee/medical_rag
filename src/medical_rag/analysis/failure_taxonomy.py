"""Failure taxonomy classification for incorrect answers.

Will classify incorrect experiment answers into failure categories (e.g.
retrieval miss, reasoning error, hallucination) for qualitative analysis.

TODO: classify_failure(result) -> str failure category.
TODO: build_failure_taxonomy(results) -> pandas.DataFrame of categorized failures.
"""
